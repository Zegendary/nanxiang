<meta charset="UTF-8">
<title>南翔馒头店#东鞋西渡#</title>
<meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, minimal-ui">
<script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.2/axios.min.js"></script>

<style>
    * {
        padding: 0;
        margin: 0;
        font-family: sans-serif;
        box-sizing: border-box;
    }
    ul,li{
        list-style: none;
    }
    body{
        position: relative;
    }
    .rules-wrapper{
        position: absolute;
        left: 0;
        top: 22px;
        background-color: #F5A623;
        padding-bottom: 2px;
        padding-right: 2px;
        animation: scaling2 0.6s infinite alternate;
    }
    .rules-wrapper .rules{
        background-color: #F4F1E3;
        color: #472516;
        border: 1px solid #000;
        font-size: 12px;
        padding: 4px 8px;
        border-left: none;
    }
    .rules-wrapper .line{
        width: 98%;
        background-color: #000;
        height: 2px;
    }
    @keyframes move {
        0% {
            transform: translateY(-5px);
        }
        0% {
            transform: translateY(0);
        }
        100% {
            transform: translateY(5px);
        }
    }
    @keyframes scaling {
        0% {
            transform: scale(0.99);
        }
        0% {
            transform: scale(1);
        }
        100% {
            transform: scale(1.02);
        }
    }
    @keyframes scaling2 {
        0% {
            transform: scale(0.9);
        }
        0% {
            transform: scale(1);
        }
        100% {
            transform: scale(1.1);
        }
    }
</style>
